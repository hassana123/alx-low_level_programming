#include "main.h"

/**
 * mul - a program that muliplies two integers
 * @a: first integer to be multiplied
 * @b: second integer to be multiplied
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
