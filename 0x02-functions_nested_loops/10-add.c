#include "main.h"

/**
 * add -  a program that takes to integers and adds them up
 * @n: first integer to be taken
 * @m: second integer to be taken
 * Return: sum of the integers
 */
int add(int n, int m)
{
	int sum;

	sum = n + m;
	return (sum);
}
