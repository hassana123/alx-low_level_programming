#include "main.h"

/**
 * _abs - computes the absolute value of integer
 * @n: the integer to check
 * Return: the absolute val of the int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-(n));
	}
}
